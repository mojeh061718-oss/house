{
  "name": "havenplan",
  "private": true,
  "version": "1.0.0",
  "description": "HavenPlan — professional 2D/3D home design studio for mobile and desktop",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
