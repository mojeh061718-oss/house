// Aggregated "extra" content packs (v2.22.0+): 100+ modern / fun / outdoor
// assets authored in src/catalog/packs/*. items.js spreads EXTRA_ITEMS into
// ITEMS so they register like any built-in item.
//
// NOTE: this file is rebuilt to import every pack once the pack modules land.
// It ships as an empty list until then so the app always builds.

export const EXTRA_ITEMS = [];
